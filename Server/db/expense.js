const models = require('../models')
const expenseModel = models.expenseModel

const create = async (amount) => {
    try {
        //st
    } catch (err) {
        //st
    }
}

const getAll = async (username) => {
    // getting staff...
}

const getOne = async (username) => {
    // getting staff
}

const update = async (username) => {
    // update staff
}

// TODO: 
// 1. creating an expense record.
// 2. get all expense specific to user
// 3. get one record anyway
// 4. updating today's expense
// 