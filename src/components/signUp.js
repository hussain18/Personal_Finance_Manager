import NavBar from './nav.js'

export default function signUp(props) {
    return <div className = "container">
        <NavBar 
            signUp = ' active'
            />

        <p>This is going to be sign Up page</p>
    </div>
}