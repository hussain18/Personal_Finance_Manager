import NavBar from './nav.js'

export default function login(props) {
    return <div className = "container">
        <NavBar 
            logIn = ' active'
            />

        <p>This is going to be log in page</p>
    </div>
}